.git
.gitignore
.dockerignore
Dockerfile

__pycache__
*.py[cod]
.pytest_cache
.ruff_cache
.venv
venv

# Local SQLite; a deployment uses Postgres via DATABASE_URL.
*.db

# The frontend is built inside the image's node stage; a host-side
# node_modules or dist would only be shadowed by that stage's own build.
web/node_modules
web/dist

tests
alembic/README
