fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
psycopg[binary]==3.2.3
alembic==1.14.0
pydantic==2.10.4
pytest==8.3.4
httpx==0.28.1
yfinance==0.2.66
# Imported directly by the Yahoo adapter for its browser-profile session:
# Yahoo's public endpoints rate-limit the default request identity in some
# environments. Pinned rather than relied on transitively through yfinance.
curl-cffi==0.13.0
